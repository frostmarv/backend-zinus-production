# ===========================================
# PRODUCTION ENVIRONMENT (Render.com)
# ===========================================
# For production deployment

NODE_ENV=production
DB_TYPE=postgres

# Render.com PostgreSQL (replace with your actual DATABASE_URL)
DATABASE_URL=postgresql://zinus_user:REPLACE_PASSWORD@dpg-REPLACE_ID.singapore-postgres.render.com/zinus_production

# Production settings (IMPORTANT!)
DB_SYNC=false
DB_LOGGING=false

# CORS - Replace with your actual frontend domains
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Render sets PORT automatically, but you can override
PORT=10000

# Optional production settings
LOG_LEVEL=info